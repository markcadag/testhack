<%= form_for appear_in, :remote => true do |f| %>
  <% if appear_in.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appear_in.errors.count, "error") %> prohibited this topic from being saved:</h2>
      <ul>
      <% appear_in.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.hidden_field :topic_id, :value => topic.id %>
      <div class="row">
        <div class="col-md-8">
          <%= f.select :course_id, Course.all.collect {|c| [ c.name, c.id ] } , {}, :id => "course_id", :style => 'width:100%;'  %>
        </div>
        <div class="col-md-2 pull-left">
          <%= f.submit 'Submit', :class => 'btn btn-xs def-btn '%>
        </div>
    </div><br/>
<% end %>